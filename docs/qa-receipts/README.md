# QA Receipts

This directory is reserved for local QA receipts generated by Kaka development commands.

Receipt JSON files are intentionally ignored in the public repository because they may contain local paths, runtime profile names, local IP addresses, simulator identifiers, physical device identifiers, and other machine-specific evidence.

Commit only redacted or synthetic receipt examples.
